#include <stdio.h>

/**
 * main - prints program name, followed by a new line followed
 * @argc: int
 * @argv: arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
